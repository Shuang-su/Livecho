{
  "name": "@livecho/railway-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline, fail-closed Railway desired-state contract for Livecho.",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "railway": "3.11.0"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
